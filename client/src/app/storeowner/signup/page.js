import OrganizationSignup from '../../../Components/Auth/OrganizationSignup';

export const metadata = {
  title: 'Create Your Organization — SikhoAurBadho',
  description: 'Sign up as a store owner to create your organization.',
};

export default function StoreOwnerSignupPage() {
  return <OrganizationSignup />;
}
